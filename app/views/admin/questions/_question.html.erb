<div class="flex space-x-5">
    <div><%= question.content%></div>
    <div class="flex">
        <!-- <%= link_to '編集', edit_admin_question_path(question), class: 'btn btnprimary mx-1' %> -->
        <%= link_to '詳細', admin_question_path(question), class: 'btn btnprimary mx-1' %>
        <%= button_to '削除', admin_question_path(question), class: 'btn btn-danger mx-1', method: :delete, date: { confirm: '削除します' } %>
    </div>
</div>